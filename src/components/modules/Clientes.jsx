import { Fragment } from "react"

import { ModuleTitle, ModuleSection, Submit, Table } from "../BasicModule"

const Clientes = () => {
    return (
        <Fragment>
            <ModuleTitle text="Clientes" />
        </Fragment>
    );
}

export default Clientes;